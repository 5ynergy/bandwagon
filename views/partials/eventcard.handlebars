<a>
<div class="ui card">
    <div class="image">
        {{!-- replace with event_image when seed data is ready --}}
    <img class="genreimage" src="{{event_image}}">

  </div>
  <div class="content">
    <div class="header"><a href="/artist/{{artist_id}}">{{User.name}}</a></div>
    <div class="description">
      <a href="/events/{{id}}">{{event_name}}</a>

    </div>
    <div class="meta">
      <a>{{address}}</a>
    </div>
    <div class="description">
      {{date}} 
      {{time}}
    </div>
  </div>
</div>
</a>

