<section>

<div class="ui card">
    <div class="card">
        <div class="image">
        <img src="/images/avatar2/large/matthew.png">
        </div>
        <div class="content">

        <div class="header">{{ event_name }} by {{ user.name }}<</div>
        {{!-- the "user.name" is not working for some reason --}}
        <div class="meta">
            <a>{{address}}</a>
        </div>
        <div class="description">
           {{ date }} {{ content }}
        </div>
        </div>
    </div>
</div>
</section>

<section>

<br>
<br>
<br>
<br>
</section>

