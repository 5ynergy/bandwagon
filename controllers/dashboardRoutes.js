// ROUTES for dashboard 


//GET event data ONLY belonging to the logged-in User
    //Include events (all data)

    //POST event (must be logged in, current user id must match profile owner id)
router.post('')

//POST event data (when artists submit "New Event")
// (must be logged in, current user id must match profile owner id)
// router.post('')
    //Include artist (name, id)