<section>

<div class="ui card">
    <div class="card">
        <div class="image">
        <img src="/images/avatar2/large/matthew.png">
        </div>
        <div class="content">
        <div class="header">{{events.event_name}}</div>
        <div class="meta">
            <a>Address Here</a>
        </div>
        <div class="description">
            <p>{{events.content}}</p>
            <p>{{events.date}}</p>
            <p>{{events.date}}</p>
            <p>{{user.name}}</p>
        </div>
        </div>
    </div>
</div>
</section>

<section>

//{{ event_name }}<br>
//{{ date }}<br>
//{{ content }}<br>
//{{ artist_name }}<br>
//</section>