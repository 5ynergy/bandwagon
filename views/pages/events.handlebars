{{!-- Can use this as a template for partials functionality --}}

<h1>All Events</h1>
<h2>See everything that's happening near you.</h2>
<div class="flex">
<div id="event-cards" class="ui stackable grid container">
    {{#each events}}
    <div class="four wide column">
    {{> eventcard}}
    </div>
    {{/each}}
</div>
</div>
<p><a href="/">Return Home</a></p>

